models:
  - name: customer_staging
    columns:
      - name: customer_id
        tests:
          - not_null
          - unique

      - name: customer_name
        tests:
          - unique

  - name: stag_orders
    columns:
      - name: order_id
        tests:
          - not_null
        # You can add more tests or descriptions here